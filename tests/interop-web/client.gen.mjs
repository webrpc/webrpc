// example v1.0.0 2c480c67fe13cd611d228efee1c10651af13443f
// --
// Code generated by webrpc-gen with javascript generator. DO NOT EDIT.
//
// webrpc-gen -schema=../../_examples/golang-basics/example.ridl -target=javascript -client -service=Example -out=./client.gen.mjs
// WebRPC description and code-gen version
export const WebRPCVersion = "v1"

// Schema version of your RIDL schema
export const WebRPCSchemaVersion = "v1.0.0"

// Schema hash generated from your RIDL schema
export const WebRPCSchemaHash = "2c480c67fe13cd611d228efee1c10651af13443f"

//
// Types
//

export var Kind;
(function (Kind) {
  Kind["USER"] = "USER"
  Kind["ADMIN"] = "ADMIN"
})(Kind || (Kind = {}))

export var Intent;
(function (Intent) {
  Intent["openSession"] = "openSession"
  Intent["closeSession"] = "closeSession"
  Intent["validateSession"] = "validateSession"
})(Intent || (Intent = {}))

export var ProtocolVersion;
(function (ProtocolVersion) {
  ProtocolVersion["v1"] = "v1"
  ProtocolVersion["v1_5"] = "v1_5"
})(ProtocolVersion || (ProtocolVersion = {}))

export var Country;
(function (Country) {
  Country["US"] = "US"
  Country["CA"] = "CA"
})(Country || (Country = {}))

export class Empty {
  constructor(_data) {
    this._data = {}
    if (_data) {
    }
  }
  
  toJSON() {
    return this._data
  }
}

export class User {
  constructor(_data) {
    this._data = {}
    if (_data) {
      this._data['id'] = _data['id']
      this._data['USERNAME'] = _data['USERNAME']
      this._data['role'] = _data['role']
      this._data['kind'] = _data['kind']
      this._data['intent'] = _data['intent']
    }
  }
  
  get id() {
    return this._data['id']
  }
  set id(value) {
    this._data['id'] = value
  }
  
  get USERNAME() {
    return this._data['USERNAME']
  }
  set USERNAME(value) {
    this._data['USERNAME'] = value
  }
  
  get role() {
    return this._data['role']
  }
  set role(value) {
    this._data['role'] = value
  }
  
  get kind() {
    return this._data['kind']
  }
  set kind(value) {
    this._data['kind'] = value
  }
  
  get intent() {
    return this._data['intent']
  }
  set intent(value) {
    this._data['intent'] = value
  }
  
  toJSON() {
    return this._data
  }
}

export class SearchFilter {
  constructor(_data) {
    this._data = {}
    if (_data) {
      this._data['q'] = _data['q']
    }
  }
  
  get q() {
    return this._data['q']
  }
  set q(value) {
    this._data['q'] = value
  }
  
  toJSON() {
    return this._data
  }
}

export class Version {
  constructor(_data) {
    this._data = {}
    if (_data) {
      this._data['webrpcVersion'] = _data['webrpcVersion']
      this._data['schemaVersion'] = _data['schemaVersion']
      this._data['schemaHash'] = _data['schemaHash']
    }
  }
  
  get webrpcVersion() {
    return this._data['webrpcVersion']
  }
  set webrpcVersion(value) {
    this._data['webrpcVersion'] = value
  }
  
  get schemaVersion() {
    return this._data['schemaVersion']
  }
  set schemaVersion(value) {
    this._data['schemaVersion'] = value
  }
  
  get schemaHash() {
    return this._data['schemaHash']
  }
  set schemaHash(value) {
    this._data['schemaHash'] = value
  }
  
  toJSON() {
    return this._data
  }
}

export class ComplexType {
  constructor(_data) {
    this._data = {}
    if (_data) {
      this._data['meta'] = _data['meta']
      this._data['metaNestedExample'] = _data['metaNestedExample']
      this._data['namesList'] = _data['namesList']
      this._data['numsList'] = _data['numsList']
      this._data['doubleArray'] = _data['doubleArray']
      this._data['listOfMaps'] = _data['listOfMaps']
      this._data['listOfUsers'] = _data['listOfUsers']
      this._data['mapOfUsers'] = _data['mapOfUsers']
      this._data['user'] = _data['user']
    }
  }
  
  get meta() {
    return this._data['meta']
  }
  set meta(value) {
    this._data['meta'] = value
  }
  
  get metaNestedExample() {
    return this._data['metaNestedExample']
  }
  set metaNestedExample(value) {
    this._data['metaNestedExample'] = value
  }
  
  get namesList() {
    return this._data['namesList']
  }
  set namesList(value) {
    this._data['namesList'] = value
  }
  
  get numsList() {
    return this._data['numsList']
  }
  set numsList(value) {
    this._data['numsList'] = value
  }
  
  get doubleArray() {
    return this._data['doubleArray']
  }
  set doubleArray(value) {
    this._data['doubleArray'] = value
  }
  
  get listOfMaps() {
    return this._data['listOfMaps']
  }
  set listOfMaps(value) {
    this._data['listOfMaps'] = value
  }
  
  get listOfUsers() {
    return this._data['listOfUsers']
  }
  set listOfUsers(value) {
    this._data['listOfUsers'] = value
  }
  
  get mapOfUsers() {
    return this._data['mapOfUsers']
  }
  set mapOfUsers(value) {
    this._data['mapOfUsers'] = value
  }
  
  get user() {
    return this._data['user']
  }
  set user(value) {
    this._data['user'] = value
  }
  
  toJSON() {
    return this._data
  }
}

export class GetUserRequest {
  constructor(_data) {
    this._data = {}
    if (_data) {
      this._data['userId'] = _data['userId']
      this._data['prefs'] = _data['prefs']
    }
  }
  
  get userId() {
    return this._data['userId']
  }
  set userId(value) {
    this._data['userId'] = value
  }
  
  get prefs() {
    return this._data['prefs']
  }
  set prefs(value) {
    this._data['prefs'] = value
  }
  
  toJSON() {
    return this._data
  }
}

export class GetUserResponse {
  constructor(_data) {
    this._data = {}
    if (_data) {
      this._data['code'] = _data['code']
      this._data['user'] = _data['user']
      this._data['profile'] = _data['profile']
    }
  }
  
  get code() {
    return this._data['code']
  }
  set code(value) {
    this._data['code'] = value
  }
  
  get user() {
    return this._data['user']
  }
  set user(value) {
    this._data['user'] = value
  }
  
  get profile() {
    return this._data['profile']
  }
  set profile(value) {
    this._data['profile'] = value
  }
  
  toJSON() {
    return this._data
  }
}

export class UploadAvatarRequest {
  constructor(_data) {
    this._data = {}
    if (_data) {
      this._data['userId'] = _data['userId']
      this._data['avatar'] = _data['avatar']
    }
  }
  
  get userId() {
    return this._data['userId']
  }
  set userId(value) {
    this._data['userId'] = value
  }
  
  get avatar() {
    return this._data['avatar']
  }
  set avatar(value) {
    this._data['avatar'] = value
  }
  
  toJSON() {
    return this._data
  }
}

export class UploadAvatarResponse {
  constructor(_data) {
    this._data = {}
    if (_data) {
      this._data['size'] = _data['size']
    }
  }
  
  get size() {
    return this._data['size']
  }
  set size(value) {
    this._data['size'] = value
  }
  
  toJSON() {
    return this._data
  }
}

export class DownloadAvatarRequest {
  constructor(_data) {
    this._data = {}
    if (_data) {
      this._data['userId'] = _data['userId']
    }
  }
  
  get userId() {
    return this._data['userId']
  }
  set userId(value) {
    this._data['userId'] = value
  }
  
  toJSON() {
    return this._data
  }
}

  
//
// Client
//

export class Example {
  constructor(hostname, fetch) {
    this.path = '/v1/Example/'
    this.hostname = hostname
    this.fetch = (input, init) => fetch(input, init)
  }

  url(name) {
    return this.hostname + this.path + name
  }
  
  ping = (headers) => {
    return this.fetch(
      this.url('Ping'),
      createHTTPRequest({}, headers)
    ).then((res) => {
      return buildResponse(res).then(_data => {
        return {
        }
      })
    })
  }
  
  status = (headers) => {
    return this.fetch(
      this.url('Status'),
      createHTTPRequest({}, headers)
    ).then((res) => {
      return buildResponse(res).then(_data => {
        return {
          status: (_data.status)
        }
      })
    })
  }
  
  version = (headers) => {
    return this.fetch(
      this.url('Version'),
      createHTTPRequest({}, headers)
    ).then((res) => {
      return buildResponse(res).then(_data => {
        return {
          version: new Version(_data.version)
        }
      })
    })
  }
  
  getUser = (args, headers) => {
    return this.fetch(
      this.url('GetUser'),
      createHTTPRequest(args, headers)
    ).then((res) => {
      return buildResponse(res).then(_data => {
        return {
          getUserResponse: new GetUserResponse(_data.getUserResponse)
        }
      })
    })
  }
  
  getUserV2 = (args, headers) => {
    return this.fetch(
      this.url('GetUserV2'),
      createHTTPRequest(args, headers)
    ).then((res) => {
      return buildResponse(res).then(_data => {
        return {
          getUserResponse: new GetUserResponse(_data.getUserResponse)
        }
      })
    })
  }
  
  findUser = (args, headers) => {
    return this.fetch(
      this.url('FindUser'),
      createHTTPRequest(args, headers)
    ).then((res) => {
      return buildResponse(res).then(_data => {
        return {
          name: (_data.name)
          , user: new User(_data.user)
        }
      })
    })
  }
  
  getIntents = (headers) => {
    return this.fetch(
      this.url('GetIntents'),
      createHTTPRequest({}, headers)
    ).then((res) => {
      return buildResponse(res).then(_data => {
        return {
          intents: (_data.intents)
        }
      })
    })
  }
  
  countIntents = (args, headers) => {
    return this.fetch(
      this.url('CountIntents'),
      createHTTPRequest(args, headers)
    ).then((res) => {
      return buildResponse(res).then(_data => {
        return {
          count: (_data.count)
        }
      })
    })
  }
  
  uploadAvatar = (args, headers) => {
    const form = new FormData()
    form.append('json', jsonEncodeUploadArgs(args))
    appendFilePart(form, 'avatar', args.avatar)
    return this.fetch(
      this.url('UploadAvatar'),
      createHTTPUploadRequest(form, headers)
    ).then((res) => {
      return buildResponse(res).then(_data => {
        return new UploadAvatarResponse(_data)
      })
    })
  }
  
  downloadAvatar = (args, headers) => {
    return this.fetch(
      this.url('DownloadAvatar'),
      createHTTPRequest(args, headers)
    ).then((res) => {
      return buildFileResponse(res)
    })
  }
  
  downloadAvatarWithMeta = (args, headers) => {
    return this.fetch(
      this.url('DownloadAvatarWithMeta'),
      createHTTPRequest(args, headers)
    ).then((res) => {
      return buildFileMetaResponse(res, 'avatar').then(_data => {
        return {
          avatar: _data.avatar
          , size: (_data.size)
          , contentType: (_data.contentType)
        }
      })
    })
  }
  
}

  
const createHTTPRequest = (body = {}, headers = {}) => {
  return {
    method: 'POST',
    headers: { ...headers, 'Content-Type': 'application/json' },
    body: JSON.stringify(body || {})
  }
}

const buildResponse = (res) => {
  return res.text().then(text => {
    let data
    try {
      data = JSON.parse(text)
    } catch(err) {
      throw { code: 'unknown', msg: `expecting JSON, got: ${text}`, status: res.status }
    }
    if (!res.ok) {
      throw data // webrpc error response
    }
    return data
  })
}

//
// File helpers
//


// jsonEncodeUploadArgs JSON-encodes upload method arguments for the leading
// "json" multipart part. File (Blob) values are serialized as null: their
// content travels in the dedicated multipart file parts instead.
const jsonEncodeUploadArgs = (obj = {}) => {
  return JSON.stringify(obj || {}, (_key, value) => {
    return value instanceof Blob ? null : value
  })
}

const createHTTPUploadRequest = (form, headers = {}) => {
  // NOTE: no explicit Content-Type header; fetch() derives the multipart
  // boundary from the FormData body.
  return { method: 'POST', headers: { ...headers }, body: form }
}

const appendFilePart = (form, name, file) => {
  if (file == null) {
    return
  }
  const filename = file instanceof File && file.name !== '' ? file.name : name
  form.append(name, file, filename)
}


// buildFileResponse reconstructs a file downloaded from a webrpc method as a
// plain object from the raw HTTP response:
//
//   {
//     name,        // filename from the Content-Disposition header, if any
//     contentType, // MIME type as claimed by the server
//     size,        // Content-Length of the file, if sent by the server
//     body,        // raw response body stream (ReadableStream), or null
//     blob()       // buffers the entire body and resolves to a Blob
//   }
// FileResponseMetaHeader carries a download method's non-file outputs,
// JSON-encoded, alongside the raw file in the response body.
const FileResponseMetaHeader = 'Webrpc-Response'

// buildFileMetaResponse reconstructs a download method's response from the
// raw file body plus the non-file outputs carried in the metadata header. A
// response without that header yields zero-valued metadata rather than an
// error, so a server that sends none still returns the file.
const buildFileMetaResponse = (res, fileField) => {
  return buildFileResponse(res).then((file) => {
    const header = res.headers.get(FileResponseMetaHeader)
    const meta = header ? JSON.parse(header) : {}
    meta[fileField] = file
    return meta
  })
}


const buildFileResponse = (res) => {
  if (!res.ok) {
    return buildResponse(res).then(() => {
      throw { code: 'unknown', msg: `expected file response, got status: ${res.status}`, status: res.status }
    })
  }
  const file = {
    contentType: res.headers.get('Content-Type') || 'application/octet-stream',
    body: res.body,
    blob: () => res.blob()
  }
  const name = fileNameFromContentDisposition(res.headers.get('Content-Disposition'))
  if (name !== undefined) {
    file.name = name
  }
  const contentLength = res.headers.get('Content-Length')
  if (contentLength !== null && /^\d+$/.test(contentLength)) {
    file.size = Number(contentLength)
  }
  return Promise.resolve(file)
}

const fileNameFromContentDisposition = (header) => {
  if (!header) {
    return undefined
  }
  // RFC 5987 extended parameter, e.g. filename*=utf-8''na%C3%AFve.png
  const extended = header.match(/filename\*=utf-8''([^;]*)/i)
  if (extended && extended[1]) {
    try {
      return decodeURIComponent(extended[1].trim())
    } catch (err) {}
  }
  const quoted = header.match(/filename="((?:[^"\\]|\\.)*)"/)
  if (quoted && quoted[1]) {
    return quoted[1].replace(/\\(.)/g, '$1')
  }
  const plain = header.match(/filename=([^;]+)/)
  if (plain && plain[1]) {
    return plain[1].trim()
  }
  return undefined
}
