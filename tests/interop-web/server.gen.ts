/* eslint-disable */
// example v1.0.0 17228855f1664011a8dd9ba38810611b9ff1c865
// --
// Code generated by Webrpc-gen with typescript generator. DO NOT EDIT.
//
// webrpc-gen -schema=../../_examples/golang-basics/example.ridl -target=typescript -server -service=Example -out=./server.gen.ts
// Webrpc description and code-gen version
export const WebrpcVersion = "v1"

// Schema version of your RIDL schema
export const WebrpcSchemaVersion = "v1.0.0"

// Schema hash generated from your RIDL schema
export const WebrpcSchemaHash = "17228855f1664011a8dd9ba38810611b9ff1c865"


//
// Server interface
//

export interface ExampleServer<Context = unknown> {
  ping(ctx: Context, req: PingRequest): Promise<PingResponse>
  status(ctx: Context, req: StatusRequest): Promise<StatusResponse>
  version(ctx: Context, req: VersionRequest): Promise<VersionResponse>
  getUser(ctx: Context, req: GetUserRequest): Promise<GetUserResponse>
  getUserV2(ctx: Context, req: GetUserRequest): Promise<GetUserResponse>
  findUser(ctx: Context, req: FindUserRequest): Promise<FindUserResponse>
  getIntents(ctx: Context, req: GetIntentsRequest): Promise<GetIntentsResponse>
  countIntents(ctx: Context, req: CountIntentsRequest): Promise<CountIntentsResponse>
  uploadAvatar(ctx: Context, req: UploadAvatarRequest): Promise<UploadAvatarResponse>
  downloadAvatar(ctx: Context, req: DownloadAvatarRequest): Promise<Blob | File>
}

//
// Schema types
//

export enum Kind {
  USER = 'USER',
  ADMIN = 'ADMIN'
}

export enum Intent {
  openSession = 'openSession',
  closeSession = 'closeSession',
  validateSession = 'validateSession'
}

export enum ProtocolVersion {
  v1 = 'v1',
  v1_5 = 'v1.5'
}

export enum Country {
  US = 'United States',
  CA = 'Canada'
}

export interface Empty {
}

export interface User {
  id: number
  USERNAME: string
  role: string
  kind: Kind
  intent: Intent
}

export interface SearchFilter {
  q: string
}

export interface Version {
  webrpcVersion: string
  schemaVersion: string
  schemaHash: string
}

export interface ComplexType {
  meta: {[key: string]: any}
  metaNestedExample: {[key: string]: {[key: string]: number}}
  namesList: Array<string>
  numsList: Array<number>
  doubleArray: Array<Array<string>>
  listOfMaps: Array<{[key: string]: number}>
  listOfUsers: Array<User>
  mapOfUsers: {[key: string]: User}
  user: User
}

export interface GetUserRequest {
  userId: number
  prefs: {[key: string]: string}
}

export interface GetUserResponse {
  code: number
  user: User
  profile: string
}

export interface UploadAvatarRequest {
  userId: number
  avatar: Blob | File
}

export interface UploadAvatarResponse {
  size: number
}

export interface DownloadAvatarRequest {
  userId: number
}

export interface PingRequest {
}

export interface PingResponse {
}

export interface StatusRequest {
}

export interface StatusResponse {
  status: boolean
}

export interface VersionRequest {
}

export interface VersionResponse {
  version: Version
}



export interface FindUserRequest {
  s: SearchFilter
}

export interface FindUserResponse {
  name: string
  user: User
}

export interface GetIntentsRequest {
}

export interface GetIntentsResponse {
  intents: Array<Intent>
}

export interface CountIntentsRequest {
  userId: number
}

export interface CountIntentsResponse {
  count: {[key in Intent]?: number}
}






//
// Server handler
//

// serveExampleRpc dispatches a web-standard Request to the Example service
// and returns a web-standard Response, or null when the request path does not
// belong to this service (so the caller can try other routes).
export const serveExampleRpc = async <Context>(service: ExampleServer<Context>, ctx: Context, request: Request): Promise<Response | null> => {
  const pathname = new URL(request.url).pathname
  if (!pathname.startsWith('/v1/Example/')) return null
  const method = pathname.slice('/v1/Example/'.length)
  if (method === '' || method.includes('/')) return null

  if (request.method !== 'POST') {
    return webrpcErrorResponse(new WebrpcBadMethodError({ cause: `unsupported HTTP method ${request.method} (only POST is allowed)` }), { Allow: 'POST' })
  }

  try {
    return await dispatchExampleRequest(service, ctx, method, request)
  } catch (err) {
    return webrpcErrorResponse(err)
  }
}

const dispatchExampleRequest = async <Context>(service: ExampleServer<Context>, ctx: Context, method: string, request: Request): Promise<Response> => {
  switch (method) {
    case 'Ping': {
      const payload: any = webrpcParseRequestBody(await webrpcRequestBodyText(request), 'PingRequest')
      const result = await service.ping(ctx, payload || {})
      return webrpcJsonResponse(result ?? {})
    }
  
    case 'Status': {
      const payload: any = webrpcParseRequestBody(await webrpcRequestBodyText(request), 'StatusRequest')
      const result = await service.status(ctx, payload || {})
      return webrpcJsonResponse(result ?? {})
    }
  
    case 'Version': {
      const payload: any = webrpcParseRequestBody(await webrpcRequestBodyText(request), 'VersionRequest')
      const result = await service.version(ctx, payload || {})
      return webrpcJsonResponse(result ?? {})
    }
  
    case 'GetUser': {
      const payload: any = webrpcParseRequestBody(await webrpcRequestBodyText(request), 'GetUserRequest')
      if (payload && !validateType(payload, "GetUserRequest")) {
        throw new WebrpcBadRequestError({ cause: "invalid argument: getUserRequest" })
      }
      const result = await service.getUser(ctx, payload || {})
      return webrpcJsonResponse(result ?? {})
    }
  
    case 'GetUserV2': {
      const payload: any = webrpcParseRequestBody(await webrpcRequestBodyText(request), 'GetUserRequest')
      if (payload && !validateType(payload, "GetUserRequest")) {
        throw new WebrpcBadRequestError({ cause: "invalid argument: getUserRequest" })
      }
      const result = await service.getUserV2(ctx, payload || {})
      return webrpcJsonResponse(result ?? {})
    }
  
    case 'FindUser': {
      const payload: any = webrpcParseRequestBody(await webrpcRequestBodyText(request), 'FindUserRequest')
      if (!("s" in payload)) {
        throw new WebrpcBadRequestError({ cause: "missing argument `s`" })
      }
      if ("s" in payload && !validateType(payload["s"], "SearchFilter")) {
        throw new WebrpcBadRequestError({ cause: "invalid argument: s" })
      }
      const result = await service.findUser(ctx, payload || {})
      return webrpcJsonResponse(result ?? {})
    }
  
    case 'GetIntents': {
      const payload: any = webrpcParseRequestBody(await webrpcRequestBodyText(request), 'GetIntentsRequest')
      const result = await service.getIntents(ctx, payload || {})
      return webrpcJsonResponse(result ?? {})
    }
  
    case 'CountIntents': {
      const payload: any = webrpcParseRequestBody(await webrpcRequestBodyText(request), 'CountIntentsRequest')
      if (!("userId" in payload)) {
        throw new WebrpcBadRequestError({ cause: "missing argument `userId`" })
      }
      if ("userId" in payload && !validateType(payload["userId"], "number")) {
        throw new WebrpcBadRequestError({ cause: "invalid argument: userId" })
      }
      const result = await service.countIntents(ctx, payload || {})
      return webrpcJsonResponse(result ?? {})
    }
  
    case 'UploadAvatar': {
      const form = await webrpcUploadFormData(request)
      const payload: any = webrpcParseRequestBody(form.get('json'), 'UploadAvatarRequest')
      webrpcFormFile(form, 'avatar', payload)
      if (payload && !validateType(payload, "UploadAvatarRequest")) {
        throw new WebrpcBadRequestError({ cause: "invalid argument: uploadAvatarRequest" })
      }
      const result = await service.uploadAvatar(ctx, payload || {})
      return webrpcJsonResponse(result ?? {})
    }
  
    case 'DownloadAvatar': {
      const payload: any = webrpcParseRequestBody(await webrpcRequestBodyText(request), 'DownloadAvatarRequest')
      if (payload && !validateType(payload, "DownloadAvatarRequest")) {
        throw new WebrpcBadRequestError({ cause: "invalid argument: downloadAvatarRequest" })
      }
      const result = await service.downloadAvatar(ctx, payload || {})
      return webrpcFileResponse(result)
    }
  
    default:
      throw new WebrpcBadRouteError({ cause: 'method not found' })
  }
}


const webrpcContentType = (request: Request): string => {
  return (request.headers.get('Content-Type') || '').split(';')[0]!.trim().toLowerCase()
}

const webrpcRequestBodyText = async (request: Request): Promise<string> => {
  if (webrpcContentType(request) !== 'application/json') {
    throw new WebrpcBadRequestError({ cause: `unsupported Content-Type '${request.headers.get('Content-Type') || ''}' (only application/json is allowed)` })
  }
  return request.text()
}

const webrpcParseRequestBody = (body: unknown, typ: string): any => {
  if (typeof body !== 'string' || body === '') {
    return {}
  }
  let data: any
  try {
    data = JSON.parse(body)
  } catch (err) {
    throw new WebrpcBadRequestError({ cause: `failed to unmarshal request data: ${err instanceof Error ? err.message : String(err)}` })
  }
  try {
    return JsonDecode(data, typ) ?? {}
  } catch (err) {
    throw new WebrpcBadRequestError({ cause: `failed to unmarshal request data: ${err instanceof WebrpcError ? String(err.cause ?? err.message) : err instanceof Error ? err.message : String(err)}` })
  }
}

const webrpcJsonResponse = (data: any): Response => {
  return new Response(JsonEncode(data), {
    status: 200,
    headers: { [WebrpcHeader]: WebrpcHeaderValue, 'Content-Type': 'application/json' },
  })
}

const webrpcErrorResponse = (err: unknown, headers?: { [key: string]: string }): Response => {
  const rpcErr = err instanceof WebrpcError ? err : new WebrpcEndpointError({ cause: err instanceof Error ? err.message : String(err) })
  const body: { [key: string]: any } = { error: rpcErr.name, code: rpcErr.code, msg: rpcErr.message, status: rpcErr.status }
  if (rpcErr.cause !== undefined) {
    body['cause'] = String(rpcErr.cause)
  }
  return new Response(JSON.stringify(body), {
    status: rpcErr.status || 400,
    headers: { ...headers, [WebrpcHeader]: WebrpcHeaderValue, 'Content-Type': 'application/json' },
  })
}


const webrpcUploadFormData = async (request: Request): Promise<FormData> => {
  if (webrpcContentType(request) !== 'multipart/form-data') {
    throw new WebrpcBadRequestError({ cause: `unsupported Content-Type '${request.headers.get('Content-Type') || ''}' (only multipart/form-data is allowed)` })
  }
  try {
    return await request.formData()
  } catch (err) {
    throw new WebrpcBadRequestError({ cause: `failed to parse multipart form: ${err instanceof Error ? err.message : String(err)}` })
  }
}

// webrpcFormFile sets the named file argument from its multipart form part,
// or removes the argument when the part is absent.
const webrpcFormFile = (form: FormData, name: string, args: any) => {
  const file = form.get(name)
  if (file instanceof Blob) {
    args[name] = file
  } else {
    delete args[name]
  }
}


// webrpcFileResponse streams the file returned by a download method as the
// raw response body, with the file's metadata carried in the headers. The
// inline Content-Disposition lets browsers preview the file in place; its
// filename still names the file on save-as.
const webrpcFileResponse = (file: Blob | File): Response => {
  if (!(file instanceof Blob)) {
    return webrpcErrorResponse(new WebrpcBadResponseError({ cause: 'download method did not return a file' }))
  }
  const headers = new Headers({
    [WebrpcHeader]: WebrpcHeaderValue,
    'Content-Type': file.type || 'application/octet-stream',
    'Content-Length': String(file.size),
    'X-Content-Type-Options': 'nosniff',
  })
  if (file instanceof File && file.name !== '') {
    headers.set('Content-Disposition', webrpcContentDisposition(file.name))
  }
  return new Response(file, { status: 200, headers })
}

// webrpcContentDisposition formats the inline Content-Disposition header,
// escaping the filename per RFC 6266 / RFC 5987.
const webrpcContentDisposition = (filename: string): string => {
  if (/^[\x20-\x21\x23-\x5b\x5d-\x7e]*$/.test(filename)) {
    return `inline; filename="${filename}"`
  }
  if (/^[\x20-\x7e]*$/.test(filename)) {
    return `inline; filename="${filename.replace(/([\\"])/g, '\\$1')}"`
  }
  return `inline; filename*=utf-8''${encodeURIComponent(filename)}`
}



//
// Server type validators
//

const JS_TYPES = [
    "bigint",
    "boolean",
    "function",
    "number",
    "object",
    "string",
    "symbol",
    "undefined"
]

const validateKind = (value: any) => {
  if (!("USER" in value) || !validateType(value["USER"], "number")) {
    return false
  }
  if (!("ADMIN" in value) || !validateType(value["ADMIN"], "number")) {
    return false
  }
  return true
}

const validateIntent = (value: any) => {
  if (!("openSession" in value) || !validateType(value["openSession"], "string")) {
    return false
  }
  if (!("closeSession" in value) || !validateType(value["closeSession"], "string")) {
    return false
  }
  if (!("validateSession" in value) || !validateType(value["validateSession"], "string")) {
    return false
  }
  return true
}

const validateProtocolVersion = (value: any) => {
  if (!("v1" in value) || !validateType(value["v1"], "string")) {
    return false
  }
  if (!("v1_5" in value) || !validateType(value["v1_5"], "string")) {
    return false
  }
  return true
}

const validateCountry = (value: any) => {
  if (!("US" in value) || !validateType(value["US"], "string")) {
    return false
  }
  if (!("CA" in value) || !validateType(value["CA"], "string")) {
    return false
  }
  return true
}

const validateEmpty = (_value: any) => {
  return true
}

const validateUser = (value: any) => {
  if (!("id" in value) || !validateType(value["id"], "number")) {
    return false
  }
  if (!("USERNAME" in value) || !validateType(value["USERNAME"], "string")) {
    return false
  }
  if (!("role" in value) || !validateType(value["role"], "string")) {
    return false
  }
  if (!("kind" in value) || !validateType(value["kind"], "Kind")) {
    return false
  }
  if (!("intent" in value) || !validateType(value["intent"], "Intent")) {
    return false
  }
  return true
}

const validateSearchFilter = (value: any) => {
  if (!("q" in value) || !validateType(value["q"], "string")) {
    return false
  }
  return true
}

const validateVersion = (value: any) => {
  if (!("webrpcVersion" in value) || !validateType(value["webrpcVersion"], "string")) {
    return false
  }
  if (!("schemaVersion" in value) || !validateType(value["schemaVersion"], "string")) {
    return false
  }
  if (!("schemaHash" in value) || !validateType(value["schemaHash"], "string")) {
    return false
  }
  return true
}

const validateComplexType = (value: any) => {
  if (!("meta" in value) || !validateType(value["meta"], "object")) {
    return false
  }
  if (!("metaNestedExample" in value) || !validateType(value["metaNestedExample"], "object")) {
    return false
  }
  if (!("namesList" in value) || !validateType(value["namesList"], "string[]")) {
    return false
  }
  if (!("numsList" in value) || !validateType(value["numsList"], "number[]")) {
    return false
  }
  if (!("doubleArray" in value) || !validateType(value["doubleArray"], "Array<string>[]")) {
    return false
  }
  if (!("listOfMaps" in value) || !validateType(value["listOfMaps"], "{[key: string]: number}[]")) {
    return false
  }
  if (!("listOfUsers" in value) || !validateType(value["listOfUsers"], "User[]")) {
    return false
  }
  if (!("mapOfUsers" in value) || !validateType(value["mapOfUsers"], "object")) {
    return false
  }
  if (!("user" in value) || !validateType(value["user"], "User")) {
    return false
  }
  return true
}

const validateGetUserRequest = (value: any) => {
  if (!("userId" in value) || !validateType(value["userId"], "number")) {
    return false
  }
  if (!("prefs" in value) || !validateType(value["prefs"], "object")) {
    return false
  }
  return true
}

const validateGetUserResponse = (value: any) => {
  if (!("code" in value) || !validateType(value["code"], "number")) {
    return false
  }
  if (!("user" in value) || !validateType(value["user"], "User")) {
    return false
  }
  if (!("profile" in value) || !validateType(value["profile"], "string")) {
    return false
  }
  return true
}

const validateUploadAvatarRequest = (value: any) => {
  if (!("userId" in value) || !validateType(value["userId"], "number")) {
    return false
  }
  if (!("avatar" in value) || !validateType(value["avatar"], "file")) {
    return false
  }
  return true
}

const validateUploadAvatarResponse = (value: any) => {
  if (!("size" in value) || !validateType(value["size"], "number")) {
    return false
  }
  return true
}

const validateDownloadAvatarRequest = (value: any) => {
  if (!("userId" in value) || !validateType(value["userId"], "number")) {
    return false
  }
  return true
}

const TYPE_VALIDATORS: { [type: string]: (value: any) => boolean } = {
  Kind: validateKind,
  Intent: validateIntent,
  ProtocolVersion: validateProtocolVersion,
  Country: validateCountry,
  Empty: validateEmpty,
  User: validateUser,
  SearchFilter: validateSearchFilter,
  Version: validateVersion,
  ComplexType: validateComplexType,
  GetUserRequest: validateGetUserRequest,
  GetUserResponse: validateGetUserResponse,
  UploadAvatarRequest: validateUploadAvatarRequest,
  UploadAvatarResponse: validateUploadAvatarResponse,
  DownloadAvatarRequest: validateDownloadAvatarRequest,
}

const validateType = (value: any, type: string) => {
  if (type === "file") {
    return value instanceof Blob;
  }
  if (type === "file[]") {
    return Array.isArray(value) && value.every((entry) => entry instanceof Blob);
  }
  if (JS_TYPES.indexOf(type) > -1) {
    return typeof value === type;
  }
  const validator = TYPE_VALIDATORS[type];
  if (!validator) {
    return false;
  }
  return validator(value)
}
//
// File helpers
//


export const JsonEncode = <T = any>(obj: T): string => {
  return JSON.stringify(obj)
}

export const JsonDecode = <T = any>(data: string | any, _typ: string = ''): T => {
  let parsed: any = data
  if (typeof data === 'string') {
    try { parsed = JSON.parse(data) } catch (err) {
      throw WebrpcBadResponseError.new({ cause: `JsonDecode: JSON.parse failed: ${(err as Error).message}` })
    }
  }
  return parsed as T
}


//
// Errors
//

type WebrpcErrorParams = { name?: string, code?: number, message?: string, status?: number, cause?: string }

export class WebrpcError extends Error {
  code: number
  status: number

  constructor(error: WebrpcErrorParams = {}) {
    super(error.message)
    this.name = error.name || 'WebrpcEndpointError'
    this.code = typeof error.code === 'number' ? error.code : 0
    this.message = error.message || `endpoint error`
    this.status = typeof error.status === 'number' ? error.status : 400
    if (error.cause !== undefined) this.cause = error.cause
    Object.setPrototypeOf(this, WebrpcError.prototype)
  }

  static new(payload: any): WebrpcError {
    return new this({ message: payload.message, code: payload.code, status: payload.status, cause: payload.cause })
  }
}


export class WebrpcEndpointError extends WebrpcError {
  constructor(error: WebrpcErrorParams = {}) {
    super(error)
    this.name = error.name || 'WebrpcEndpoint'
    this.code = typeof error.code === 'number' ? error.code : 0
    this.message = error.message || `endpoint error`
    this.status = typeof error.status === 'number' ? error.status : 400
    if (error.cause !== undefined) this.cause = error.cause
    Object.setPrototypeOf(this, WebrpcEndpointError.prototype)
  }
}

export class WebrpcRequestFailedError extends WebrpcError {
  constructor(error: WebrpcErrorParams = {}) {
    super(error)
    this.name = error.name || 'WebrpcRequestFailed'
    this.code = typeof error.code === 'number' ? error.code : -1
    this.message = error.message || `request failed`
    this.status = typeof error.status === 'number' ? error.status : 400
    if (error.cause !== undefined) this.cause = error.cause
    Object.setPrototypeOf(this, WebrpcRequestFailedError.prototype)
  }
}

export class WebrpcBadRouteError extends WebrpcError {
  constructor(error: WebrpcErrorParams = {}) {
    super(error)
    this.name = error.name || 'WebrpcBadRoute'
    this.code = typeof error.code === 'number' ? error.code : -2
    this.message = error.message || `bad route`
    this.status = typeof error.status === 'number' ? error.status : 404
    if (error.cause !== undefined) this.cause = error.cause
    Object.setPrototypeOf(this, WebrpcBadRouteError.prototype)
  }
}

export class WebrpcBadMethodError extends WebrpcError {
  constructor(error: WebrpcErrorParams = {}) {
    super(error)
    this.name = error.name || 'WebrpcBadMethod'
    this.code = typeof error.code === 'number' ? error.code : -3
    this.message = error.message || `bad method`
    this.status = typeof error.status === 'number' ? error.status : 405
    if (error.cause !== undefined) this.cause = error.cause
    Object.setPrototypeOf(this, WebrpcBadMethodError.prototype)
  }
}

export class WebrpcBadRequestError extends WebrpcError {
  constructor(error: WebrpcErrorParams = {}) {
    super(error)
    this.name = error.name || 'WebrpcBadRequest'
    this.code = typeof error.code === 'number' ? error.code : -4
    this.message = error.message || `bad request`
    this.status = typeof error.status === 'number' ? error.status : 400
    if (error.cause !== undefined) this.cause = error.cause
    Object.setPrototypeOf(this, WebrpcBadRequestError.prototype)
  }
}

export class WebrpcBadResponseError extends WebrpcError {
  constructor(error: WebrpcErrorParams = {}) {
    super(error)
    this.name = error.name || 'WebrpcBadResponse'
    this.code = typeof error.code === 'number' ? error.code : -5
    this.message = error.message || `bad response`
    this.status = typeof error.status === 'number' ? error.status : 500
    if (error.cause !== undefined) this.cause = error.cause
    Object.setPrototypeOf(this, WebrpcBadResponseError.prototype)
  }
}

export class WebrpcServerPanicError extends WebrpcError {
  constructor(error: WebrpcErrorParams = {}) {
    super(error)
    this.name = error.name || 'WebrpcServerPanic'
    this.code = typeof error.code === 'number' ? error.code : -6
    this.message = error.message || `server panic`
    this.status = typeof error.status === 'number' ? error.status : 500
    if (error.cause !== undefined) this.cause = error.cause
    Object.setPrototypeOf(this, WebrpcServerPanicError.prototype)
  }
}

export class WebrpcInternalErrorError extends WebrpcError {
  constructor(error: WebrpcErrorParams = {}) {
    super(error)
    this.name = error.name || 'WebrpcInternalError'
    this.code = typeof error.code === 'number' ? error.code : -7
    this.message = error.message || `internal error`
    this.status = typeof error.status === 'number' ? error.status : 500
    if (error.cause !== undefined) this.cause = error.cause
    Object.setPrototypeOf(this, WebrpcInternalErrorError.prototype)
  }
}

export class WebrpcClientAbortedError extends WebrpcError {
  constructor(error: WebrpcErrorParams = {}) {
    super(error)
    this.name = error.name || 'WebrpcClientAborted'
    this.code = typeof error.code === 'number' ? error.code : -8
    this.message = error.message || `request aborted by client`
    this.status = typeof error.status === 'number' ? error.status : 400
    if (error.cause !== undefined) this.cause = error.cause
    Object.setPrototypeOf(this, WebrpcClientAbortedError.prototype)
  }
}

export class WebrpcStreamLostError extends WebrpcError {
  constructor(error: WebrpcErrorParams = {}) {
    super(error)
    this.name = error.name || 'WebrpcStreamLost'
    this.code = typeof error.code === 'number' ? error.code : -9
    this.message = error.message || `stream lost`
    this.status = typeof error.status === 'number' ? error.status : 400
    if (error.cause !== undefined) this.cause = error.cause
    Object.setPrototypeOf(this, WebrpcStreamLostError.prototype)
  }
}

export class WebrpcStreamFinishedError extends WebrpcError {
  constructor(error: WebrpcErrorParams = {}) {
    super(error)
    this.name = error.name || 'WebrpcStreamFinished'
    this.code = typeof error.code === 'number' ? error.code : -10
    this.message = error.message || `stream finished`
    this.status = typeof error.status === 'number' ? error.status : 200
    if (error.cause !== undefined) this.cause = error.cause
    Object.setPrototypeOf(this, WebrpcStreamFinishedError.prototype)
  }
}


//
// Schema errors
//

export class UserNotFoundError extends WebrpcError {
  constructor(error: WebrpcErrorParams = {}) {
    super(error)
    this.name = error.name || 'UserNotFound'
    this.code = typeof error.code === 'number' ? error.code : 1000
    this.message = error.message || `User not found`
    this.status = typeof error.status === 'number' ? error.status : 404
    if (error.cause !== undefined) this.cause = error.cause
    Object.setPrototypeOf(this, UserNotFoundError.prototype)
  }
}

export class UnauthorizedError extends WebrpcError {
  constructor(error: WebrpcErrorParams = {}) {
    super(error)
    this.name = error.name || 'Unauthorized'
    this.code = typeof error.code === 'number' ? error.code : 2000
    this.message = error.message || `Unauthorized access`
    this.status = typeof error.status === 'number' ? error.status : 401
    if (error.cause !== undefined) this.cause = error.cause
    Object.setPrototypeOf(this, UnauthorizedError.prototype)
  }
}

export class PermissionDeniedError extends WebrpcError {
  constructor(error: WebrpcErrorParams = {}) {
    super(error)
    this.name = error.name || 'PermissionDenied'
    this.code = typeof error.code === 'number' ? error.code : 3000
    this.message = error.message || `Permission denied`
    this.status = typeof error.status === 'number' ? error.status : 403
    if (error.cause !== undefined) this.cause = error.cause
    Object.setPrototypeOf(this, PermissionDeniedError.prototype)
  }
}

export enum errors {
  WebrpcEndpoint = 'WebrpcEndpoint',
  WebrpcRequestFailed = 'WebrpcRequestFailed',
  WebrpcBadRoute = 'WebrpcBadRoute',
  WebrpcBadMethod = 'WebrpcBadMethod',
  WebrpcBadRequest = 'WebrpcBadRequest',
  WebrpcBadResponse = 'WebrpcBadResponse',
  WebrpcServerPanic = 'WebrpcServerPanic',
  WebrpcInternalError = 'WebrpcInternalError',
  WebrpcClientAborted = 'WebrpcClientAborted',
  WebrpcStreamLost = 'WebrpcStreamLost',
  WebrpcStreamFinished = 'WebrpcStreamFinished',
  UserNotFound = 'UserNotFound',
  Unauthorized = 'Unauthorized',
  PermissionDenied = 'PermissionDenied',
}

export enum WebrpcErrorCodes {
  WebrpcEndpoint = 0,
  WebrpcRequestFailed = -1,
  WebrpcBadRoute = -2,
  WebrpcBadMethod = -3,
  WebrpcBadRequest = -4,
  WebrpcBadResponse = -5,
  WebrpcServerPanic = -6,
  WebrpcInternalError = -7,
  WebrpcClientAborted = -8,
  WebrpcStreamLost = -9,
  WebrpcStreamFinished = -10,
  UserNotFound = 1000,
  Unauthorized = 2000,
  PermissionDenied = 3000,
}

export const webrpcErrorByCode: { [code: number]: any } = {
  [0]: WebrpcEndpointError,
  [-1]: WebrpcRequestFailedError,
  [-2]: WebrpcBadRouteError,
  [-3]: WebrpcBadMethodError,
  [-4]: WebrpcBadRequestError,
  [-5]: WebrpcBadResponseError,
  [-6]: WebrpcServerPanicError,
  [-7]: WebrpcInternalErrorError,
  [-8]: WebrpcClientAbortedError,
  [-9]: WebrpcStreamLostError,
  [-10]: WebrpcStreamFinishedError,
  [1000]: UserNotFoundError,
  [2000]: UnauthorizedError,
  [3000]: PermissionDeniedError,
}



//
// Webrpc
//

export const WebrpcHeader = "Webrpc"

export const WebrpcHeaderValue = "webrpc;typescript;example@v1.0.0"

type WebrpcGenVersions = {
  WebrpcGenVersion: string;
  codeGenName: string;
  codeGenVersion: string;
  schemaName: string;
  schemaVersion: string;
};

export function VersionFromHeader(headers: Headers): WebrpcGenVersions {
  const headerValue = headers.get(WebrpcHeader)
  if (!headerValue) {
    return {
      WebrpcGenVersion: "",
      codeGenName: "",
      codeGenVersion: "",
      schemaName: "",
      schemaVersion: "",
    };
  }

  return parseWebrpcGenVersions(headerValue)
}

function parseWebrpcGenVersions(header: string): WebrpcGenVersions {
  const versions = header.split(";")
  if (versions.length < 3) {
    return {
      WebrpcGenVersion: "",
      codeGenName: "",
      codeGenVersion: "",
      schemaName: "",
      schemaVersion: "",
    };
  }

  const [_, WebrpcGenVersion] = versions[0]!.split("@")
  const [codeGenName, codeGenVersion] = versions[1]!.split("@")
  const [schemaName, schemaVersion] = versions[2]!.split("@")

  return {
    WebrpcGenVersion: WebrpcGenVersion ?? "",
    codeGenName: codeGenName ?? "",
    codeGenVersion: codeGenVersion ?? "",
    schemaName: schemaName ?? "",
    schemaVersion: schemaVersion ?? "",
  };
}

