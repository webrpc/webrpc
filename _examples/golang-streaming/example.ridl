webrpc = v1

name = golang-streaming
version = v0.0.1


message User
  - id: uint64
  - username: string

service ExampleService
  - Ping()

  - GetUser(id: uint64) => (user: User)

  # single request, streaming response body
  - Download(filee: string) => stream (beep: string)

  - Status() => (status: bool)
