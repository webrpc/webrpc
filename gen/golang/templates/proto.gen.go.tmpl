{{define "proto"}}
package proto

import (
  "bytes"
  "context"
  "encoding/json"
  "io"
  "io/ioutil"
  "net/http"
  "net/url"
  "strings"
  "time"

  "github.com/pkg/errors"
  "github.com/webrpc/webrpc/lib/webrpc-go"
)

{{template "types" .}}

{{template "client" .}}

{{template "server" .}}

{{template "helpers" .}}

{{end}}
